# Public Supabase configuration. Safe for browser use when paired with RLS.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only secrets will be introduced only when a phase requires them.
# Never prefix server secrets with NEXT_PUBLIC_.
