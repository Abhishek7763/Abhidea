# Phase 8 Reader Experience — next chunk

Status: IMPLEMENTATION IN PROGRESS

This checkpoint covers the next Reader experience layer after the initial typography controls and demo content.

Planned in this chunk:
- reading progress indicator
- browser-native text-to-speech (no paid external API)
- play / pause / resume / stop controls
- speech speed control
- paragraph/section progress status
- active reading segment highlight
- optional auto-follow that respects reduced motion
- share action with Web Share API and clipboard fallback
- graceful fallback when browser speech synthesis is unavailable

Production main remains untouched until the chunk passes repository verification and visual testing.
